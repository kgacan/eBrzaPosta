


<div class="sadrzajDiv">
    <div style="display:inline-block; padding:10px; margin:0 0 0 27%">
        <input style="width:325px" class="form-control form-control-lg" id="inputSifra" type="text" placeholder="Unesite broj pošiljke...">
    </div>
    <div style="display:inline-block; padding:10px;">
        <a id="myBtn" modal-poziv='da' modalUrl="/Dostave/PratiDostavu?sifra=" class="btn btn-outline-primary btn-lg">Traži</a>
    </div>


        <img src="~/images/fast_delivery.png" style="width:300px; height:200px; padding:5px; margin:10% auto; display:block; opacity: 0.6;" />




    <!-- The Modal -->
    <div id="myModal" class="modal">

        <!-- Modal content -->
        <div class="modal-content">
            <div class="modal-header">
                <h3 style="padding:5px;">Rezultat pretrage</h3>
                <span class="close">&times;</span>
            </div>
            <div class="modal-body" style="margin:60px auto; font-size:1.1em;">
            </div>
            <div class="modal-footer">
                <div class="logo" style="padding:0 10px">
                    <img src="~/images/fast_delivery.png" style="width:45px; height:40px; padding:5px;" />
                    <span style="font-size:1.3em;">BrzaPošta</span>
                </div>
            </div>
        </div>

    </div>

</div>


<script>
    // Get the modal
    var modal = document.getElementById("myModal");

    // Get the button that opens the modal
    //var btn = document.getElementById("myBtn");

    // Get the <span> element that closes the modal
    var span = document.getElementsByClassName("close")[0];

    // When the user clicks the button, open the modal
    $("a[modal-poziv='da']").click(function () {
        modal.style.display = "block";

        var btn = $(this);
        var url = btn.attr("modalUrl");

        var sifraPaketa = $("#inputSifra").val().trim();
        url += sifraPaketa;

        $.get({
            url: url,
            success: function (rez) {
                $(".modal-body").html(rez);
            }
        });
    });


    // When the user clicks on <span> (x), close the modal
    span.onclick = function () {
        modal.style.display = "none";
    }

    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function (event) {
        if (event.target == modal) {
            modal.style.display = "none";
        }
    }
</script>






@*<script>
        $("button[pretrazi='da']").click(function () {

            var sifraPaketa = $("#inputSifra").val().trim()
            var url = "/Dostave/PratiDostavu?sifra=" + sifraPaketa;
            var rez = "rezultati";

            $.get(url,
                function (rezultat) {
                    $("#" + rez).html(rezultat);
                }
            )
        });
    </script>*@